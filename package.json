{
  "name": "swordforge-root",
  "private": true,
  "scripts": {
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm install && npx vite build",
    "postinstall": "cd backend && npm install && npx prisma generate || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "socket.io": "^4.7.4",
    "bcryptjs": "^2.4.3"
  }
}
